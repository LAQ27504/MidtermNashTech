public enum RequestStatus
{
    Approved,
    Rejected,
    Waiting,
}

public enum UserType
{
    SuperUser,
    NormalUser,
}

public enum BorrowBookStatus
{
    Waiting,
    Approved,
    Rejected,
    Borrowed,
    Returned,
}
